# TRUSTDOC Frontend Environment Variables
# URL pointing to the running FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional Supabase integration (leave empty if using local backend auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
